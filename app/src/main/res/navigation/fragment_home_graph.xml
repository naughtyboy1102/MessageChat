<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_home_graph"
    app:startDestination="@id/chatOverviewFragment">

    <fragment
        android:id="@+id/chatOverviewFragment"
        android:name="com.example.messagechat.ui.home.ChatOverviewFragment"
        android:label="chat_overview_fragment"
        tools:layout="@layout/chat_overview_fragment" >
        <action
            android:id="@+id/action_chatOverviewFragment_to_contactsFragment"
            app:destination="@id/contactsFragment" />
    </fragment>

    <fragment
        android:id="@+id/contactsFragment"
        android:name="com.example.messagechat.ui.home.ContactsFragment"
        android:label="contacts_fragment"
        tools:layout="@layout/contacts_fragment">
        <action
            android:id="@+id/action_contactsFragment_to_chatOverviewFragment"
            app:destination="@id/chatOverviewFragment"
            app:popUpTo="@id/chatOverviewFragment"
            app:popUpToInclusive="true" />
    </fragment>
</navigation>